-- CTRL +  SHIFT + ENTER = 전체 실행
-- CTRL + ENTER = 한줄 실행
-- 대, 소문자 구분이 크게 없다.

SHOW DATABASES;

USE world;

SHOW TABLES;

SELECT 3 % 2;

SELECT "Hello World";

SELECT 10 = 10; -- true = 1
SELECT 10 = 11; -- false == 0
SELECT 10 != 11; -- true == 1

SELECT 10 <> 11; -- 크지도 작지도 않다